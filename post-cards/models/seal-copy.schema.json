{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hat-im.github.io/post-cards/models/seal-copy.schema.json",
  "title": "Postcards — Seal Copy",
  "description": "post-cards/data/seals/seal-copy.json. Text content each seal type in seals.json draws from, passed to PostcardSeals.buildSealExtras.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "cities": { "type": "array", "items": { "type": "string" }, "description": "Used by the cds seal type." },
    "sloganCancels": { "type": "array", "items": { "type": "string" }, "description": "Used by the sloganCancel seal type." },
    "permitPrefix": { "type": "string" },
    "permitNumbers": { "type": "array", "items": { "type": "integer" }, "description": "Used with permitPrefix by the permitIndicia seal type." },
    "parsMessages": { "type": "array", "items": { "type": "string" }, "description": "Used by the parsSticker seal type." },
    "nixieReasons": { "type": "array", "items": { "type": "string" }, "description": "Used by the nixieLabel seal type." },
    "ibiCaption": { "type": "string", "description": "Used by the ibi2d seal type." },
    "trackingPrefix": { "type": "string", "description": "Used by the trackingBarcode seal type." },
    "morseCode": { "type": "string", "pattern": "^[.\\- /]+$", "description": "Used by the fim seal type; parsed by parseMorseTokens." },
    "serviceRequestedOptions": { "type": "array", "items": { "type": "string" }, "description": "Used by the serviceRequested seal type." }
  }
}
