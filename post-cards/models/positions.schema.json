{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hat-im.github.io/post-cards/models/positions.schema.json",
  "title": "Postcards — Positions",
  "description": "post-cards/data/layout/positions.json. Candidate seal/sticker anchor points, as fractions (0-1) of the card width/height. Sticker placement filters these against layoutTextZones()'s occupied rects at render time; seals are placed unconstrained.",
  "type": "object",
  "required": ["positions"],
  "additionalProperties": false,
  "properties": {
    "positions": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["x", "y"],
        "additionalProperties": false,
        "properties": {
          "x": { "type": "number", "minimum": 0, "maximum": 1 },
          "y": { "type": "number", "minimum": 0, "maximum": 1 }
        }
      }
    }
  }
}
